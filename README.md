This Project is for creating a user service where user can register, login authentication part etc
this will use FastAPI + Mysql 

Dependencies used are ( installed using uv )
 fastapi, uvicorn, sqlalchemy, pymysql ,python-dotenv, passlib[bcrypt] ,python-jose


 To run the project 

 uv run uvicorn app.main:app --reload